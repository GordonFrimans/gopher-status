# 🐹 GopherStatus — Система мониторинга доступности сервисов

**GopherStatus** — это высокопроизводительный сервис для мониторинга времени безотказной работы (uptime) веб-сайтов и API. Проект построен на современной архитектуре с использованием **Go (gRPC + REST)** на бэкенде и **React** на фронтенде.

Проект создавался с упором на отказоустойчивость, безопасную конкурентную работу с данными и правильное использование паттернов многопоточности Go.

## 🚀 Ключевые возможности

- **Мультитенантность (Изоляция данных):** Каждый пользователь имеет свой собственный защищенный дашборд и список мониторинга.
- **Двойной API (gRPC + REST):** Внутренняя коммуникация может происходить по сверхбыстрому gRPC, а для фронтенда и сторонних интеграций развернут REST API через `grpc-gateway`.
- **Кастомный Worker Pool:** Фоновая проверка сайтов реализована через собственный пул горутин с лимитированием потоков, защитой от паник (recover) и балансировкой задач через каналы.
- **Безопасность:** JWT-авторизация через gRPC Unary Interceptor, хэширование паролей с помощью `bcrypt`.
- **UI/UX:** Современный и отзывчивый интерфейс написан на React (Vite) с использованием библиотеки Mantine.

## 🛠 Технологический стек

### Backend (Go)
- **Язык:** Go (Golang)
- **API:** gRPC, Protocol Buffers (protobuf), `grpc-gateway` (транскодинг gRPC в REST)
- **Асинхронность:** Goroutines, Channels, Tickers, `sync.WaitGroup`, `sync.RWMutex`
- **Безопасность:** `golang-jwt/jwt`, `golang.org/x/crypto/bcrypt`
- **Хранилище:** Thread-safe In-Memory Storage (с перспективой миграции на PostgreSQL)

### Frontend (React)
- **Фреймворк:** React 19 + Vite
- **UI Библиотека:** Mantine (`@mantine/core`), Tabler Icons
- **Архитектура:** Защищенные маршруты (Protected Routes), HTTP Fetch API с Bearer токенами.

### Инфраструктура
- **Docker & Docker Compose:** Контейнеризация обоих сервисов для быстрого развертывания.

---

## 🧠 Архитектурные решения (Under the Hood)

Данный проект демонстрирует решение ряда сложных инженерных задач:

1. **Concurrency Management (Управление конкурентностью):** Хранилище данных реализовано в памяти с использованием `sync.RWMutex` для оптимизации блокировок (множественные параллельные чтения при единичной записи).
   
2. **Паттерн Worker Pool:**
   Вместо создания новой горутины на каждый проверяемый сайт (что может привести к утечке памяти и DDoS-эффекту), реализован фиксированный пул воркеров. Они принимают задачи из буферизированного канала, безопасно выполняют HTTP-запросы с подменой User-Agent и отдают результаты в канал `results`.
   
3. **Middleware (gRPC Interceptors):**
   Авторизация вынесена на уровень gRPC Unary Interceptor. Перехватчик валидирует JWT-токен до того, как запрос попадет в хендлер, и прокидывает `login` пользователя через `context.Context` для обеспечения изоляции данных.

4. **Self-DDoS Protection:**
   Планировщик (Scheduler) обновляет статус задачи *до* отправки её в Worker Pool, чтобы избежать дублирования проверок в случае, если целевой сервер отвечает слишком долго.


---

## 📡 API Endpoints (REST Gateway)

Сервер автоматически транслирует REST HTTP запросы в gRPC методы.

**Авторизация:**

* `POST /v1/auth/users` — Регистрация нового пользователя (принимает `login` и `password`).
* `POST /v1/auth/login` — Вход и получение JWT токена.

**Мониторинг (Требуется заголовок `Authorization: Bearer <token>`):**

* `GET /v1/monitors` — Получить список сайтов текущего пользователя.
* `POST /v1/monitors` — Добавить новый сайт в мониторинг (принимает `url`, `name`, `interval`).
* `DELETE /v1/monitors/{id}` — Удалить сайт из мониторинга.

---
*Сделано с ❤️ на Go.*

